<Window x:Class="R_3_8_1.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:R_3_8_1"
             mc:Ignorable="d" 
             Title="Экспорт"
             Height="200" Width="200">
    <StackPanel>
        <Label Content="Выберете тип экспорта"
               Margin="5"/>
        <Button Content="ifc"
                Margin="5"
                Padding="5"
                Command="{Binding ExportIFC}"/>
        <Button Content="nwc"
                Margin="5"
                Padding="5"
                Command="{Binding ExportNWC}"/>
        <Button Content="Immage"
                Margin="5"
                Padding="5"
                Command="{Binding ExportImmage}"/>

    </StackPanel>
</Window>
